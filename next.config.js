/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
	images: {
		domains: ['fonoactivebackend.uplevelcap.cl', 'bambino.bold-themes-cdn.com'],
	},
}

module.exports = nextConfig
